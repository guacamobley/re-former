<% unless @user.errors.blank? %>
  <p>
    <%= "#{pluralize(@user.errors.count,"error")} were detected.  These are the errors: " %>
    <ul>
      <% @user.errors.full_messages.each do |error| %>
      <li> <%= error %> </li>
      <% end %>
    </ul>
  </p>
<% end %>
